# 项目记忆: ireader

## 项目信息
- 项目名称: ireader
- 创建时间: 2026-07-02

## 通用要求
## 每轮开发默认流程（自动执行）
每轮特性开发、问题定位解决、bugfix，默认执行如下操作（除非用户明确指定跳过）：
1. **自动静态检查**：修改相关代码后，确认无显示缺陷（语法/类型/导入错误）。
2. **类型与语法检查**：
   - 执行 `node --check` 检查涉及的后端/前端 JS/TS 文件
   - 执行 `tsc --noEmit`（backend 和 frontend）检查类型错误
3. **自动提交推送**：在检查全部通过后，执行 `git add . && git commit -m "中文描述" && git push`
4. **自动部署**：默认执行 `bash deploy.sh` 进行部署（或 `bash scripts/auto-archive-deploy.sh`）
