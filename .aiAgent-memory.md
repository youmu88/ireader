# 项目记忆: ireader

## 项目信息
- 项目名称: ireader
- 创建时间: 2026-07-02

## 通用要求
## 每轮开发默认流程（自动执行）
每轮特性开发、问题定位解决、bugfix，默认执行如下操作（除非用户明确指定跳过）：
1. **自动静态检查**：修改相关代码后，确认无显示缺陷（语法/类型/导入错误）。
2. **类型与语法检查**：
   - 执行 `node --check` 检查涉及的后端/前端 JS/TS 文件
   - 执行 `tsc --noEmit`（backend 和 frontend）检查类型错误
3. **自动提交推送**：在检查全部通过后，执行 `git add . && git commit -m "中文描述" && git push`
4. **自动部署**：默认执行 `bash deploy.sh` 进行部署（或 `bash scripts/auto-archive-deploy.sh`）
## 修复说明（2026-07-02）
此前"每轮开发默认流程"存在3个缺陷，本轮已修复：
1. **规则边界模糊** — 未覆盖查询/调研类任务，导致第61轮（纯查询）跳过所有检查
2. **"自动部署"语义歧义** — 写的是"自动部署"但实际是Agent手动执行脚本，被误解为CI/CD
3. **步骤顺序过死** — 要求显式`node --check`，但`tsc --noEmit`已等价覆盖

## 每轮默认流程（修正版 v2）
**适用范围**：特性开发、问题定位修复、bugfix、查询调研 均适用。

### 查询/调研类任务（无代码修改）
- 跳过静态检查/类型检查/Git提交/部署
- 在 finish_report 中注明"本次为查询任务，无代码修改，已跳过开发流程"

### 开发/修复类任务（有代码修改）
1. **静态检查**：修改代码后执行 `tsc --noEmit`（backend + frontend），等价覆盖 `node --check` 语法检查
2. **Git 提交推送**：检查通过后 `git add . && git commit -m "中文描述" && git push`
3. **部署**：执行 `bash deploy.sh` 构建并部署上线
   - ⚠️ 此为Agent手动部署脚本，非CI/CD自动触发
   - 如用户有CI/CD需求，需额外搭建（如GitHub Actions）
